# MONAD REPORT — 008 (skill monad 0.1.1)
تاریخ: ۱۴۰۵/۰۶/۲۷ (2026-09-18) — فرمان: «skill را برای شاپفا آماده کن»

## ۱. چه چیزی فهمیدم؟
- RED حالت تسک (DATA، پروژهٔ آزمایشی، تسک «دستور list»): عامل با skill 0.1.0 تسک را درست هدف گرفت (سنجه: خطوط خروجی ۰→۲، DEPLOY). یعنی کنترل شکست نخورد؛ پس فقط یک خط شفاف‌سازی اضافه شد، نه بخش تازه.
- نقص واقعی از بررسی شاپفا (DATA): `docs/` در shopfa-core gitignore است؛ skill 0.1.0 فایل‌های PROPOSAL/REPORT را آن‌جا می‌نوشت → برای تیم گم می‌شد.
- شاپفا (DATA): CLAUDE.md ۲۳۴ خطی، ~۶۰ skill پروژه‌ای، `tests/` با یک تست ad-hoc، بدون هدف `make test`؛ docker روی این ماشین نصب نیست.

## ۲. چه چیزی ساختم؟
- skill 0.1.1: (۱) هدف = تسکِ کاربر اگر داده شده، وگرنه یک پله بالاتر؛ (۲) پوشهٔ خروجی `<out>` = جای اعلام‌شده در CLAUDE.md/AGENTS.md، وگرنه `.claude/monad/` اگر docs gitignore است، وگرنه `docs/monad/`؛ (۳) هرگز URL/DB/credential تولید؛ اجرا فقط با dev setup مستند؛ اجرا نشد → UNKNOWN. تست به‌روز شد.
- نصب در شاپفا: کپی به `~/Web/shopfa-core/.claude/skills/monad/SKILL.md` (copy نه symlink: `.claude/` آن‌جا team-shared است) + یک ردیف در جدول مسیریابی CLAUDE.md شاپفا. **commit نشده** (مخزن تیم؛ تصمیم علی).

## ۳. چه چیزی اکنون قابل استفاده است؟
- در شاپفا: `/monad /Users/ali/Web/shopfa-core propose only` اجرا شد (GREEN): سطح L4 با شواهد (۴۸۵۶ کامیت، ۱۰ نویسنده)، L2 نه (بدون دستور تست رسمی)، ۱ خطای parse در `module_contact/action.contact.php:96` بازتولید شد؛ پیشنهاد: هدف `make test` (php -l + tests/*.php) + اصلاح یک‌خطی. فایل: `shopfa-core/.claude/monad/PROPOSAL-2026-09-18.md`.

## ۴. چه چیزی بهتر شد؟
- 0.1.0 → 0.1.1: فایل‌ها دیگر در پوشهٔ gitignore گم نمی‌شوند؛ مرز تولید صریح شد. روی شاپفا هر دو قاعده اجرا شد (FACT).

## ۵. چه چیزی شکست خورد / مسدود است؟
- L1/L3 شاپفا روی این ماشین سنجش‌پذیر نیست (docker نیست) → UNKNOWN ماند، درست.
- «یک گام» همچنان تفسیرپذیر (از گزارش ۰۰۷).

## ۶. چه چیزی یاد گرفتم؟
- وقتی کنترل شکست نمی‌خورد، قاعده ننویس؛ فقط پیش‌فرض را صریح کن.
- محیط پروژه (gitignore، team-shared بودن .claude) خودش منبع قاعده است.

## ۷. قدم بعدی چیست؟
- علی در شاپفا: پیشنهاد را بخواند؛ اگر موافق است `/monad /Users/ali/Web/shopfa-core` (چرخهٔ کامل) روی branch.
CONTINUE.
