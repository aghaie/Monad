# MONAD REPORT — 017 (خودآزماییِ قانون اساسی از UNVERIFIED درآمد، بی‌آنکه مُهر لاستیکی شود)
تاریخ: ۱۴۰۵/۰۷/۰۱ (2026-09-21، شبانه، چرخهٔ چهارم) — Engine: همین نشست

## ۱. چه چیزی فهمیدم؟
- FACT: در هر ۲۵ تکرار گذشته، خط `constitutional self-check: UNVERIFIED` تکرار شده بود و کسی به آن دست نزده بود. علتش این بود که حلقه دو اصل را در کد با `None` می‌داد: «اصلاح واقعی است نه ظاهری» و «راه پاکیزه‌تر جسته شد».
- FACT: این دو اصل، برخلاف هشت اصل دیگر، از ساختِ حلقه به دست نمی‌آیند؛ داوری می‌خواهند. نوشتن `True` در کد برای آن‌ها دقیقاً همان خودفریبی است که خودشان منع می‌کنند.
- FACT: این خط در «یاد گرفتم» دفن شده بود و در فهرست `blocked` نمی‌آمد؛ پس یک هشدار بود که هیچ‌کس نمی‌دیدش.

## ۲. چه چیزی ساختم؟
- `self_check` اکنون پاسخ این دو اصل را از ادعای ثبت‌شدهٔ Engine می‌خواند (ادعای دارای تگ `selfcheck:<اصل>` و `answer:true|false`)، همراه با دلیل و منبعش؛ و اگر آن ادعا کهنه شود، پاسخ هم باطل می‌شود. **هیچ خودتأییدی از مدرکش عمر بیشتری ندارد.**
- نتیجهٔ غیرVALID دیگر در «یاد گرفتم» دفن نمی‌شود؛ در `blocked` می‌آید.
- سنجهٔ `unverified_principles` (کمتر بهتر) با مبنای تاریخیِ درست: تکرارهای پیشین واقعاً دو اصل بی‌پاسخ داشتند، پس پیش‌فرضشان ۲ است نه ۰ — گذشته را زیبا نمی‌کنیم.
- `--expires <روز>` برای `monad claim`، تا پاسخ‌های خودسنجی تاریخ انقضا داشته باشند.
- دو پاسخِ امشب با دلیل ثبت شد (اعتبار ۷ روز): اصلاحِ واقعی، چون هر سه چرخهٔ پیشین یک سنجهٔ بیرونی را جابه‌جا کرد؛ و راه پاکیزه‌تر، چون پیش از هر ساختن، جایگزین ارزان‌تر آزموده شد (فرمان کوچک به‌جای اسکریپت موقت، خواندن فهرست تغییرات تنزیل به‌جای ساختن طبقه‌بند، وانهادن جایگزینی پیکرهٔ قرآن به علی).

## ۳. چه چیزی اکنون قابل استفاده است؟
```
.venv/bin/python -m monad claim RATIONAL_ANALYSIS "<منبع>" "<دلیل>" --tags selfcheck:reform_not_appearance,answer:true --expires 7
.venv/bin/python -m monad iterate     # اکنون: constitutional self-check: VALID
```

## ۴. چه چیزی بهتر شد؟
- خودآزمایی: `UNVERIFIED` (۲۵ تکرار پیاپی) → **VALID**، بر پایهٔ دو ادعای دارای منبع و انقضا.
- تکرار ۲۶: **INCONCLUSIVE** و این را پنهان نمی‌کنم: جهشِ `unverified_principles: 2 → 0` در تکرار ۲۵ رخ داد، ولی مبنای مقایسهٔ آن تکرار (۲۴) هنوز این سنجه را نداشت، پس بی‌امتیاز ماند؛ و تکرار ۲۶ با ۲۵ برابر است. سنجه را اصلاح کردم، ولی تکرار گذشته را دوباره امتیاز ندادم.
- تست‌ها ۳۴ passed / ۱ skipped؛ ادعاها ۶۲ → ۶۴؛ تناقض ۰؛ سنجهٔ کاربرد دست‌نخورده (۵).

## ۵. چه چیزی شکست خورد / مسدود است؟
- خطر شناخته‌شده: پاسخ‌های خودسنجی را خودِ مناد می‌نویسد. سه مهار گذاشته شد — دلیل و منبع الزامی، انقضای هفت‌روزه، و دیده‌شدن در `blocked` وقتی VALID نیست. مهار چهارم دست علی است: خواندن و رد کردن.
- BLOCKED (بی‌تغییر، تصمیم علی): جایگزینی پیکرهٔ قرآن با ویرایش ۱٫۱ تنزیل.

## ۶. قدم بعد
حلقه می‌گوید sweep. کارِ ساختنیِ بی‌نیاز از علی تمام شده؛ چرخهٔ بعد اگر چیزی برای ساختن نبود، فقط خواندن و داوری خواهد بود (مادهٔ ۱۸).
