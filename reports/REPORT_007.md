# MONAD REPORT — 007 (iteration 13)
تاریخ: ۱۴۰۵/۰۶/۲۷ (2026-09-18) — فرمان: «یک skill/agent قابل رونمایی عمومی به اسم monad بساز که هر پروژه‌ای را در هر سطحی بهبود دهد»

## ۱. چه چیزی فهمیدم؟
- یک skill، «فرایند» را ثابت می‌کند نه «قضاوت» را؛ پس ارزشش در جلوگیری از خطاهای تکراری است. باید اول دید عامل بدون skill کجا خطا می‌کند (RED)، بعد نوشت (GREEN).
- RED (DATA، پروژهٔ آزمایشی «خرج»: ۲۱ خط پایتون با ۴ نقص، بدون تست): عامل بدون skill شش اصلاح را یک‌جا انجام داد، هیچ خطِ پایه‌ای پیش از تغییر نگرفت، پیشنهادی ننوشت، گزارش فقط در چت بود، commit بدون WHY/WHAT/EXPECTED/ACTUAL، و «بهتر شد» بدون عددِ قبل.
- GREEN (DATA، همان پروژه، نسخهٔ تازه، با skill): چهار نقص را پیش از تغییر بازتولید کرد، سطح L1 را با شواهد تعیین کرد، `docs/monad/PROPOSAL` نوشت، یک گام (L1→L2) با تست‌اول، سنجش قبل/بعد (کرش ۴/۴ → ۱/۴؛ تست: هیچ → ۴ OK)، حکم DEPLOY، commit با قالب کامل، `docs/monad/REPORT` با برچسب FACT.

## ۲. چه چیزی ساختم؟
- `skills/monad/SKILL.md` (~۹۹۰ کلمه): چرخهٔ OBSERVE → LEVEL (L0–L5) → PROPOSE → EXECUTE → MEASURE → REPORT؛ قالب commit؛ هفت سرفصل گزارش؛ برچسب FACT/HYPOTHESIS/UNKNOWN؛ جدول توجیه‌های رایج و پرچم‌های قرمز.
- نصب: symlink به `~/.claude/skills/monad` → `/monad <path>` در همین دستگاه فعال است.
- ثبت در Skill Registry (`monad 0.1.0 ACTIVE`) + `tests/test_skill_monad.py`. مجموع: ۲۲ passed + ۱ skipped.

## ۳. چه چیزی اکنون قابل استفاده است؟
- `/monad <مسیر پروژه>` در Claude Code؛ `/monad <مسیر> propose only` برای فقط پیشنهاد. برای رونمایی عمومی: پوشهٔ `skills/monad` را کپی/لینک کنند.

## ۴. چه چیزی بهتر شد؟
- iteration 13 نسبت به 12: DEPLOY (active_skills ۸ → ۹).
- در پروژهٔ آزمایشی: با skill، هر ادعای «بهتر شد» عدد قبل/بعد داشت؛ بدون skill هیچ‌کدام.

## ۵. چه چیزی شکست خورد / مسدود است؟
- «یک گام» هنوز تفسیرپذیر است: عامل با skill سه نقص را زیر «یک نامتغیر» جمع کرد. قابل دفاع بود، ولی حفرهٔ احتمالی است؛ با آزمون‌های بیشتر بسته می‌شود.
- فقط یک سناریوی آزمایشی (پروژهٔ L1). سطوح L3–L5 و پروژه‌های بزرگ هنوز آزموده نشده‌اند.

## ۶. چه چیزی یاد گرفتم؟
- بدون RED، نمی‌دانستم کدام قاعده لازم است؛ چهار قاعده از پنج قاعدهٔ اصلی skill مستقیماً از خطاهای مشاهده‌شده آمد.

## ۷. قدم بعدی چیست؟
- `/monad` را روی یک پروژهٔ واقعی علی (مثلاً monad-v2) اجرا کن و حفره‌های جدید را در جدول توجیه‌ها ببند.
- استفادهٔ واقعی میزان همچنان صفر: `python3 -m monad serve` روشن است.

CONTINUE.
