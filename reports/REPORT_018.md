# MONAD REPORT — 018 (ایرادِ سنجهٔ دیشبِ خودم: داوری به متن گره خورد، نه به نشانی)
تاریخ: ۱۴۰۵/۰۷/۰۱ (2026-09-21، شبانه، چرخهٔ پنجم) — Engine: همین نشست

## ۱. چه چیزی فهمیدم؟
- FACT: سنجهٔ `judged_sources` که دیشب ساختم، منبع را در سطح **نشانی** داوری‌شده می‌شمرد. یعنی اگر فردا tanzil.net/updates/ خبر نسخهٔ ۱٫۲ را منتشر کند، حلقه ادعای خام تازه را ذخیره می‌کند ولی سنجه همچنان می‌گوید «داوری شده» و هیچ‌کس سراغ متن تازه نمی‌رود.
- FACT: این دقیقاً همان منبعی را کور می‌کرد که دیشب برای دیدنِ تغییرها اضافه کردم.
- FACT: امروز هر چهار منبع به تازه‌ترین نسخهٔ متنشان گره خورده‌اند (۳+۳+۱+۳ ادعای داوری)، پس اصلاح، عددِ امروز را عوض نمی‌کند — فقط کوریِ فردا را می‌بندد.

## ۲. چه چیزی ساختم؟
- در `MonadLoop.learn`: منبع وقتی «داوری‌شده» است که دست‌کم یک ادعای `extracted` داشته باشد که مدرکش **تازه‌ترین** ادعای خام همان نشانی باشد. تغییرِ صفحه ⇒ بازگشت به «داوری‌نشده» و نام‌بردنش در قدم بعد.
- تست: صفحه عوض می‌شود ⇒ `judged_sources` از ۱ به ۰ برمی‌گردد و `next_step` همان نشانی را می‌گوید.
- `MONAD_ROADMAP.md`: نیمهٔ «ارزیابی منبع» از v0.3 انجام‌شده علامت خورد؛ نیمهٔ «دریافت از GitHub و مقاله‌ها» باز ماند.

## ۳. چه چیزی اکنون قابل استفاده است؟
```
.venv/bin/python -m monad iterate     # هر تغییر در صفحه‌های زیر نظر، دوباره داوری می‌خواهد
```

## ۴. چه چیزی بهتر شد؟
- تکرار ۲۷: **INCONCLUSIVE**، و راست این است که باید هم می‌بود: این اصلاح از خطای فردا جلوگیری می‌کند، عددِ امروز را جابه‌جا نمی‌کند. رنگش نمی‌کنم.
- تست‌ها ۳۴ passed / ۱ skipped؛ ادعاها ۶۷؛ تناقض ۰؛ سنجهٔ کاربرد دست‌نخورده (۵).

## ۵. چه چیزی شکست خورد / مسدود است؟
- دو چرخهٔ پیاپی INCONCLUSIVE (تکرارهای ۲۶ و ۲۷). طبق قاعدهٔ کار شبانه، چرخهٔ بعد فقط خواندن و داوری است، نه کد.
- BLOCKED (بی‌تغییر، تصمیم علی): جایگزینی پیکرهٔ قرآن با ویرایش ۱٫۱.

## ۶. قدم بعد
چرخهٔ بی‌کد: خواندن دوبارهٔ منابع و داوری هر تغییر، و اگر تغییری نبود، گزارشِ «چیزی برای ساختن نبود».
