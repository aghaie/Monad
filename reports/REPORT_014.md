# MONAD REPORT — 014 (اولین داوری منابع؛ سنجهٔ «منابع داوری‌شده»)
تاریخ: ۱۴۰۵/۰۷/۰۱ (2026-09-21، شبانه) — Engine: همین نشست (تصمیم بنیان‌گذار ۱۴۰۵/۰۶/۳۰)

## ۱. چه چیزی فهمیدم؟
- FACT: `next_step` حلقه از تکرار ۱۹ روی «داوری منابع (نیازمند Engine)» گیر کرده بود، چون `judge_source` فقط وقتی کار می‌کند که Engine همان لحظه پاسخ بدهد؛ SessionEngine پاسخ را به صف می‌برد (Pending) و صفحه دیگر «تغییر» نمی‌کند، پس داوری هرگز نمی‌رسید.
- FACT: سه منبع `data/sources.txt` فقط *خوانده* شده بودند؛ از میان آن‌ها تنها docs.python.org ۳ ادعای استخراج‌شده داشت و دو منبع تنزیل هیچ.
- FACT: چهار ادعای خام از `tanzil.net/docs/quran_text_types` متنِ یکسان ولی sha متفاوت دارند — یادگار پیش از اصلاحِ «هش روی متن استخراج‌شده»؛ پس از اصلاح، هر سه منبع در تکرارهای ۱۹ تا ۲۱ `unchanged` دیده می‌شوند.
- FACT (سنجش نو): دادهٔ محلی قرآن ۶۲۳۶ آیه و ۱۱۴ سوره دارد، sha256 آن با MANIFEST می‌خواند، هیچ نشانهٔ وقفِ عثمانی (U+06D6..U+06ED) ندارد و ۶۲۳۵ آیه حرکت‌گذاری دارد (تنها ۴۲:۲ «عسق» بی‌حرکت) → با «خط سادهٔ» تنزیل می‌خواند، نه خط عثمانی.

## ۲. چه چیزی ساختم؟
- `python -m monad claim <ORIGIN> <source> <text> [--conf --evidence --tags]` — تنها راهِ ثبت داوری Engine بدون اسکریپت موقت؛ برچسب `engine:session` خودکار افزوده می‌شود (۱۱ خط در `monad/cli.py`، ۲ تست).
- سنجهٔ `judged_sources` در حلقه: شمار منابعِ `sources.txt` که دست‌کم یک ادعای `extracted` دارند؛ و زنجیرهٔ `next_step` دو حلقهٔ تازه گرفت: «این منبع را داوری کن: <url>» و سپس «این نامعلوم را حل کن: …» (۱ تست).
- ۷ ادعای واقعی: ۴ ادعای DATA از داوری دو منبع تنزیل (با `evidence` به ادعای خام)، ۱ EMPIRICAL_RESULT دربارهٔ صحت دادهٔ قرآن، ۱ EMPIRICAL_RESULT دربارهٔ تکرار ادعاهای خام، ۱ UNKNOWN صریح.

## ۳. چه چیزی اکنون قابل استفاده است؟
```
.venv/bin/python -m monad claim DATA <url> "<آنچه منبع می‌گوید>" --evidence <id ادعای خام> --tags extracted
.venv/bin/python -m monad iterate     # قدم بعد اکنون منبع/نامعلومِ مشخص را نام می‌برد
```

## ۴. چه چیزی بهتر شد؟
- تکرار ۲۰: **INCONCLUSIVE** (`no measurable change`) — داوری انجام شده بود ولی هیچ سنجه‌ای آن را نمی‌دید. همین، خودِ شکاف بود.
- تکرار ۲۱: **DEPLOY** — `judged_sources: 0 → 3`؛ تست‌ها ۳۲ passed / ۱ skipped.
- ادعاها ۴۱ → ۵۰؛ تناقض ۰؛ ادعای کهنه ۰؛ سنجهٔ کاربرد دست‌نخورده (۵، همان استفادهٔ واقعی Mizan) — مادهٔ ۷ رعایت شد.

## ۵. چه چیزی شکست خورد / مسدود است؟
- UNKNOWN (ثبت‌شده، نه پنهان): آیا `data/quran/quran-simple.csv` همان نسخهٔ ۱٫۱ تنزیل است؟ MANIFEST فقط مبدأ واردکردن را دارد، نه شمارهٔ نسخه. پاسخ نیازمند دریافت متن رسمی و مقایسهٔ sha است.
- BLOCKED (بی‌تغییر): داوری خودکار منابع؛ تا وقتی Engine = نشست است، هر داوری دستِ من است و در تکرار بی‌سرپرست انجام نمی‌شود.
- HYPOTHESIS (۰٫۶، از گزارش ۱۳ باز مانده): کاربرِ ۵ ادعای Mizan خودِ مناد است نه انسان؛ تا علی خودش کار نکند، «استفادهٔ انسانی» صفر است.

## ۶. قدم بعد
همان که حلقه می‌گوید: حل نامعلومِ نسخهٔ متن قرآن — دریافت متن رسمی تنزیل و مقایسهٔ sha، یا افزودن شمارهٔ نسخه به MANIFEST اگر مدرکی پیدا شد. چرخهٔ بعدی شبانه همین را برمی‌دارد.
