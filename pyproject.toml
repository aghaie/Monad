[project]
name = "monad"
version = "0.1.0"
description = "MONAD — a living engine for understanding, building and improving software"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest"]

[tool.pytest.ini_options]
testpaths = ["tests", "products"]
